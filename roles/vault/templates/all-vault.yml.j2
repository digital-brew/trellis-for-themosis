{% from 'render_vars.j2' import custom_vars, default_var with context -%}

# Documentation: https://roots.io/trellis/docs/vault/
{{ default_var('vault_mail_password', vault_vars['all'], vault_passwords_length) }}
{{ custom_vars(vault_vars['all'].keys() | difference(vault_vars_default['all']), vault_vars['all'], 0, true) -}}
